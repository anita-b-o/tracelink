import { AlertTriangle } from "lucide-react";
import { StatusBadge } from "@/components/ui/status-badge";
import type { RelationshipDetail } from "@/lib/api/types";

export function RelationshipDetailCard({item}:{item:RelationshipDetail}) { return <>{item.status==="CONTRADICTED"&&<div className="danger-banner"><AlertTriangle size={15}/> Opposing evidence is preserved for this relationship.</div>}<div className="detail-grid"><div className="detail-item"><span>Source</span>{item.source_entity.canonical_name}</div><div className="detail-item"><span>Target</span>{item.target_entity.canonical_name}</div><div className="detail-item"><span>Type</span>{item.type}</div><div className="detail-item"><span>Status</span><StatusBadge status={item.status}/></div><div className="detail-item"><span>Confidence</span>{Math.round(item.confidence*100)}%</div><div className="detail-item"><span>Validity</span>{item.temporal_start??"Unknown"} – {item.temporal_end??"present"}</div></div><section className="detail-section"><h3>Evidence</h3>{item.evidence.map(e=><article className={`evidence-card ${e.evidence_type==="CONTRADICTING"?"contradicting":""}`} key={e.id}><StatusBadge status={e.evidence_type}/><p>{e.preview??"No preview"}</p></article>)}</section></>; }
