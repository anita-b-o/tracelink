envVarGroups:
  - name: tracelink-staging-auth
    envVars:
      - key: AUTH_JWT_SECRET
        generateValue: true
      - key: AUTH_TOKEN_PEPPER
        generateValue: true

services:
  - type: web
    name: tracelink-web
    runtime: docker
    dockerfilePath: ./infra/docker/frontend.Dockerfile
    dockerContext: .
    dockerCommand: node server.js
    autoDeployTrigger: off
    healthCheckPath: /api/health/ready
    envVars:
      - key: APP_ENV
        value: staging
      - key: BACKEND_INTERNAL_HOSTPORT
        fromService:
          type: pserv
          name: tracelink-api
          property: hostport
  - type: pserv
    name: tracelink-api
    runtime: docker
    dockerfilePath: ./infra/docker/backend.Dockerfile
    dockerContext: .
    autoDeployTrigger: off
    preDeployCommand: alembic upgrade head
    envVars:
      - key: APP_ENV
        value: staging
      - key: DATABASE_URL
        fromDatabase:
          name: tracelink-postgres
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: keyvalue
          name: tracelink-redis
          property: connectionString
      - fromGroup: tracelink-staging-auth
      - key: CORS_ALLOWED_ORIGINS
        fromService:
          type: web
          name: tracelink-web
          envVarKey: RENDER_EXTERNAL_URL
      - key: ALLOWED_HOSTS
        fromService:
          type: pserv
          name: tracelink-api
          property: host
      - key: COOKIE_SECURE
        value: "true"
      - key: REGISTRATION_ENABLED
        value: "true"
      - key: METRICS_BEARER_TOKEN
        generateValue: true
      - key: EMBEDDING_PROVIDER
        value: openai
      - key: LLM_PROVIDER
        value: openai
      - key: OPENAI_API_KEY
        sync: false
  - type: worker
    name: tracelink-worker
    runtime: docker
    dockerfilePath: ./infra/docker/backend.Dockerfile
    dockerContext: .
    dockerCommand: celery --app tracelink.jobs.celery_app:celery_app worker --loglevel=INFO
    autoDeployTrigger: off
    envVars:
      - key: APP_ENV
        value: staging
      - key: DATABASE_URL
        fromDatabase:
          name: tracelink-postgres
          property: connectionString
      - key: REDIS_URL
        fromService: {type: keyvalue, name: tracelink-redis, property: connectionString}
      - key: CELERY_BROKER_URL
        fromService: {type: keyvalue, name: tracelink-redis, property: connectionString}
      - key: CELERY_RESULT_BACKEND
        fromService: {type: keyvalue, name: tracelink-redis, property: connectionString}
      - fromGroup: tracelink-staging-auth
      - key: CORS_ALLOWED_ORIGINS
        fromService:
          type: web
          name: tracelink-web
          envVarKey: RENDER_EXTERNAL_URL
      - key: ALLOWED_HOSTS
        fromService:
          type: pserv
          name: tracelink-api
          property: host
      - key: COOKIE_SECURE
        value: "true"
      - key: REGISTRATION_ENABLED
        value: "true"
      - key: EMBEDDING_PROVIDER
        value: openai
      - key: LLM_PROVIDER
        value: openai
      - key: OPENAI_API_KEY
        sync: false
  - type: worker
    name: tracelink-outbox
    runtime: docker
    dockerfilePath: ./infra/docker/backend.Dockerfile
    dockerContext: .
    dockerCommand: python -m tracelink.outbox_dispatcher
    autoDeployTrigger: off
    envVars:
      - key: APP_ENV
        value: staging
      - key: DATABASE_URL
        fromDatabase:
          name: tracelink-postgres
          property: connectionString
      - key: REDIS_URL
        fromService: {type: keyvalue, name: tracelink-redis, property: connectionString}
      - key: CELERY_BROKER_URL
        fromService: {type: keyvalue, name: tracelink-redis, property: connectionString}
      - key: CELERY_RESULT_BACKEND
        fromService: {type: keyvalue, name: tracelink-redis, property: connectionString}
      - fromGroup: tracelink-staging-auth
      - key: CORS_ALLOWED_ORIGINS
        fromService:
          type: web
          name: tracelink-web
          envVarKey: RENDER_EXTERNAL_URL
      - key: ALLOWED_HOSTS
        fromService:
          type: pserv
          name: tracelink-api
          property: host
      - key: COOKIE_SECURE
        value: "true"
      - key: REGISTRATION_ENABLED
        value: "true"
      - key: EMBEDDING_PROVIDER
        value: openai
      - key: LLM_PROVIDER
        value: openai
      - key: OPENAI_API_KEY
        sync: false
  - type: keyvalue
    name: tracelink-redis
    ipAllowList: []
    plan: starter
    maxmemoryPolicy: noeviction

databases:
  - name: tracelink-postgres
    plan: basic-256mb
    ipAllowList: []
    databaseName: tracelink
    user: tracelink
    postgresMajorVersion: "17"
